# 必须依赖
torch==2.4.1          # Transformers>=5 要求 PyTorch>=2.4
transformers>=4.38.0
datasets==2.20.0      # 与 pyarrow<18 兼容，arm64 轮子现成
pandas>=1.5.3,<2.2    # Py311/arm64 wheels现成，且满足 mlrun<2.2 约束
safetensors>=0.4.2
tqdm>=4.66.1
Cython<3
tokenizers>=0.13.3   # Hugging Face 的高性能 tokenizer
accelerate>=0.27.0   # Trainer 依赖，用于多设备调度
pyarrow>=15.0.0,<18  # datasets 2.x 仍兼容，避免 18+ ARM 构建缺失
mlflow>=2.12.1       # 取代 MLRun，K8s 友好的开源跟踪

# 可选依赖（视具体模型需求）
sentencepiece==0.2.0
aiohttp>=3.9.5
prometheus_client==0.22.1
